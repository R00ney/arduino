Settings for using duemilanove on new ubuntu

First Time Only:
$ sudo usermod -a -G dialout <username>
$ sudo chmod a+rw /dev/ttyUSB0

Every Upload:
Tools> Boad> Arduino Duemilanove or Deicimila
Tools> Processor > ATmega168
Tools> Port> /dev/ttyUSB0


for chuck monitor
Serial Monitor at 115200 baud rate